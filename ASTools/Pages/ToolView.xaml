<UserControl x:Class="ASTools.Pages.ToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mdview="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             xmlns:local="clr-namespace:ASTools.Pages"
             xmlns:lcliz="clr-namespace:ASTools.Unit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="{Binding Source={x:Static lcliz:LocalizationProvider.Instance}, Path=[ButtonBack]}" Command="{Binding BackCommand}" Style="{StaticResource BackBtn}" HorizontalAlignment="Left" Margin="5"/>
        <DockPanel Grid.Row="1">
            <Border Width="62" Height="62" CornerRadius="8" Margin="10,4,4,4" DockPanel.Dock="Left">
                <Border.Background>
                    <ImageBrush Stretch="Fill" ImageSource="{Binding Tool.Icon}"/>
                </Border.Background>
            </Border>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Margin="0,0,10,0">
                <Button Content="{Binding Source={x:Static lcliz:LocalizationProvider.Instance}, Path=[ButtonDelete]}" Style="{StaticResource CancelBtn}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <Button Content="{Binding Source={x:Static lcliz:LocalizationProvider.Instance}, Path=[ButtonEdit]}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <Button Content="{Binding Source={x:Static lcliz:LocalizationProvider.Instance}, Path=[ButtonToolLaunch]}" Style="{StaticResource AccentBtn}" Command="{Binding RunToolCommand}" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center" Margin="10,0,0,0">
                <TextBlock Text="{Binding Tool.Name}" Style="{StaticResource TextBlok_Subtitle}"/>
                <TextBlock Text="{Binding Tool.Author}" Style="{StaticResource TextBlok_Body}"/>
            </StackPanel>
        </DockPanel>
        <TabControl Grid.Row="2" Margin="5, 10, 5, 0">
            <TabItem Header="{Binding Source={x:Static lcliz:LocalizationProvider.Instance}, Path=[ToolPageAboutTitle]}" Style="{StaticResource ToolInfoItem}">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBlock Text="{Binding Tool.Description}" Margin="10" TextWrapping="Wrap" Foreground="{StaticResource TextColor_Primary}"/>
                        <Image Source="{Binding Tool.Banner}" />
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="{Binding Source={x:Static lcliz:LocalizationProvider.Instance}, Path=[ToolPageDocumentationTitle]}" Style="{StaticResource ToolInfoItem}" Visibility="{Binding isRMHawe}">
                <mdview:MarkdownViewer Markdown="{Binding Tool.MdReadme}" Foreground="{StaticResource TextColor_Primary}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
