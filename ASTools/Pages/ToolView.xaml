<UserControl x:Class="ASTools.Pages.ToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mdview="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             xmlns:local="clr-namespace:ASTools.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="Back" Command="{Binding BackCommand}" Width="100" Height="20" HorizontalAlignment="Left"/>
        <DockPanel Grid.Row="1">
            <Border Width="62" Height="62" CornerRadius="8" Margin="4" DockPanel.Dock="Left">
                <Border.Background>
                    <ImageBrush Stretch="Fill" ImageSource="{Binding Tool.Icon}"/>
                </Border.Background>
            </Border>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button>Удалить</Button>
                <Button>Изменить</Button>
                <Button>Запустить</Button>
            </StackPanel>
            <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                <TextBlock Text="{Binding Tool.Name}" FontSize="24" FontFamily="Segoe UI Variable Text Semibold" Margin="10,0,0,0"/>
                <TextBlock Text="{Binding Tool.Author}" />
            </StackPanel>
        </DockPanel>
        <TabControl Grid.Row="2">
            <TabItem Header="Описание">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBlock Text="{Binding Tool.Description}" Margin="10" TextWrapping="Wrap"/>
                        <Image Source="{Binding Tool.Banner}" />
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Документация" Visibility="{Binding isRMHawe}">
                <mdview:MarkdownViewer Markdown="{Binding Tool.MdReadme}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
