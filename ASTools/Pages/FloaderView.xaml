<UserControl x:Class="ASTools.Pages.FloaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ASTools.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel>
            <StackPanel Orientation="Horizontal" Margin="5,0" VerticalAlignment="Center" DockPanel.Dock="Right">
                <RadioButton GroupName="View" Content="Таблица" IsChecked="True"/>
                <RadioButton GroupName="View" Content="строка"/>
            </StackPanel>
            <Button DockPanel.Dock="Right" Margin="5,0">Delete</Button>
            <Button DockPanel.Dock="Right" Margin="5,0">Edit</Button>
            <TextBlock Text="{Binding Title}" FontSize="24" FontFamily="Segoe UI Variable Text Semibold"/>
        </DockPanel>
        <TabControl Style="{StaticResource FloaderControl}" 
                Grid.Row="1"
                ItemsSource="{Binding Items}"
                SelectedItem="{Binding SelectedItem}" Margin="0,10,0,0">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Header}"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ListBox ItemsSource="{Binding Content}"
                         BorderThickness="0"
                         Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Width="56" Height="56" CornerRadius="8" VerticalAlignment="Center" Margin="4">
                                        <Border.Background>
                                            <ImageBrush Stretch="Fill" ImageSource="{Binding Icon}"/>
                                        </Border.Background>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Medium" Foreground="{StaticResource TextColor}" Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding Author}" FontSize="12" FontWeight="Regular" Foreground="{StaticResource TextColorSecodary}"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding TagsDisplay}" FontSize="12" FontWeight="Regular" Foreground="{StaticResource TextColorSecodary}" Grid.Column="2" VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource Selection}"/>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </DataTemplate>
            </TabControl.ContentTemplate>
            <TabControl.ItemContainerStyle>
                <Style TargetType="{x:Type TabItem}" BasedOn="{StaticResource FloaderItem}"/>
            </TabControl.ItemContainerStyle>
        </TabControl>
    </Grid>
</UserControl>
