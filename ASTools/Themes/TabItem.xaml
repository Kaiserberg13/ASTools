<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ASTools">
    
    <Style TargetType="{x:Type TabItem}" x:Key="FloaderItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border x:Name="TabBorder" 
                            CornerRadius="15" 
                            Background="Transparent" 
                            Padding="16, 0"
                            Height="30"
                            BorderBrush="{StaticResource OutlineColor}"
                            BorderThickness="1"
                            Margin="4"
                            >
                        <TextBlock x:Name="Content" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <ContentPresenter ContentSource="Header"/>
                        </TextBlock>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource HoverColor}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource SelectedColor}"/>
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{StaticResource AcentColor}"/>
                            <Setter TargetName="Content" Property="Foreground" Value="{StaticResource AcentColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>