<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Foreground" Value="{StaticResource TextColor_Tertiary}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="BorderDown"
                            BorderBrush="{StaticResource OutlineColor_Down_Default}"
                            CornerRadius="4"
                            BorderThickness="0,0,0,1">
                        <Border x:Name="Border"
                                BorderBrush="{StaticResource OutlineColor_Default}"
                                Background="{StaticResource ItemColor_Default}"
                                CornerRadius="4"
                                BorderThickness="1,1,1,0">
                            <ContentPresenter Margin="10,5" />
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource ItemColor_Hover}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="BorderDown" Property="BorderBrush" Value="{StaticResource AccentColor_Default}" />
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource ItemColor_Default}" />
                            <Setter Property="Foreground" Value="{StaticResource TextColor_Primary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>